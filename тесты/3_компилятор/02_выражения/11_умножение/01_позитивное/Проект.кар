
стат метод Запустить():
	Консоль.Вывод(2 * 3)
	Консоль.Вывод("\н")
	Консоль.Вывод(0ш4 * 0ш5)
	Консоль.Вывод("\н")
	Консоль.Вывод(6.0 * 7.0)
	Консоль.Вывод("\н")

	Консоль.Вывод(Целое8(8) * Целое8(2))
	Консоль.Вывод("\н")
	Консоль.Вывод(Целое16(9) * Целое16(2))
	Консоль.Вывод("\н")
	Консоль.Вывод(Целое32(10) * Целое32(2))
	Консоль.Вывод("\н")
	Консоль.Вывод(Целое64(11) * Целое64(2))
	Консоль.Вывод("\н")

	Консоль.Вывод(Счётное8(12) * Счётное8(3))
	Консоль.Вывод("\н")
	Консоль.Вывод(Счётное16(13) * Счётное16(3))
	Консоль.Вывод("\н")
	Консоль.Вывод(Счётное32(14) * Счётное32(3))
	Консоль.Вывод("\н")
	Консоль.Вывод(Счётное64(15) * Счётное64(3))
	Консоль.Вывод("\н")

	Консоль.Вывод(Дробное32(16.0) * Дробное32(4.0))
	Консоль.Вывод("\н")
	Консоль.Вывод(Дробное64(17.0) * Дробное64(4.0))
	Консоль.Вывод("\н")
