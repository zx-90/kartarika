
стат метод Запустить():
	Консоль.Вывод((Целое8(7) / 2) ! 1)
	Консоль.Вывод("\н")
	Консоль.Вывод((Целое16(9) / 0ш2) ! 1)
	Консоль.Вывод("\н")

	Консоль.Вывод((Счётное8(11) / 2) ! 1)
	Консоль.Вывод("\н")
	Консоль.Вывод((Счётное16(13) / 0ш2) ! 1)
	Консоль.Вывод("\н")

	Консоль.Вывод(Дробное32(15.0) / 2)
	Консоль.Вывод("\н")
	Консоль.Вывод(Дробное32(17.0) / 0ш2)
	Консоль.Вывод("\н")
	Консоль.Вывод(Дробное64(19.0) / 2.0)
	Консоль.Вывод("\н")
