
стат метод Запустить():
	Консоль.Вывод((7 / 0).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((0ш09 / 0ш0).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод(6.0 / 0.0)
	Консоль.Вывод("\н")

	Консоль.Вывод((Целое8(7) / Целое8(0)).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((Целое16(9) / Целое16(0)).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((Целое32(11) / Целое32(0)).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((Целое64(13) / Целое64(0)).ПустойЛи())
	Консоль.Вывод("\н")

	Консоль.Вывод((Счётное8(13) / Счётное8(0)).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((Счётное16(16) / Счётное16(0)).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((Счётное32(19) / Счётное32(0)).ПустойЛи())
	Консоль.Вывод("\н")
	Консоль.Вывод((Счётное64(22) / Счётное64(0)).ПустойЛи())
	Консоль.Вывод("\н")

	Консоль.Вывод(Дробное32(17.0) / Дробное32(0.0))
	Консоль.Вывод("\н")
	Консоль.Вывод(Дробное64(18.0) / Дробное64(0.0))
	Консоль.Вывод("\н")
