
стат метод Запустить():
	Консоль.Вывод((Целое8(3) % 2) ! 0)
	Консоль.Вывод("\н")
	Консоль.Вывод((Целое16(5) % 0ш3) ! 1)
	Консоль.Вывод("\н")

	Консоль.Вывод((Счётное8(7) % 4) ! 1)
	Консоль.Вывод("\н")
	Консоль.Вывод((Счётное16(9) % 0ш5) ! 1)
	Консоль.Вывод("\н")
