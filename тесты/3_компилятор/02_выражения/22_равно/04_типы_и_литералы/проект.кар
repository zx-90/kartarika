
стат метод Запустить():
	Консоль.Вывод(Целое8(2) == 2)
	Консоль.Вывод("\н")
	Консоль.Вывод(Целое16(3) == 0ш2)
	Консоль.Вывод("\н")

	Консоль.Вывод(Счётное8(3) == 2)
	Консоль.Вывод("\н")
	Консоль.Вывод(Счётное16(2) == 0ш2)
	Консоль.Вывод("\н")

	Консоль.Вывод(Дробное32(2.0) == 2)
	Консоль.Вывод("\н")
	Консоль.Вывод(Дробное32(3.0) == 0ш2)
	Консоль.Вывод("\н")
	Консоль.Вывод(Дробное64(2.0) == 2.0)
	Консоль.Вывод("\н")
