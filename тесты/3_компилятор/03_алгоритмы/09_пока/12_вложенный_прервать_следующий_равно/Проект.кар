
стат метод Запустить():
	// Эта часть для проверки выхода из всех циклов.
	поле А = 0
	пока А мш 5:
		А = А + 1
		Консоль.Вывод("А = ")
		Консоль.Вывод(А)
		Консоль.Вывод("\н")

		поле Б = 0
		пока Б мш 5:
			Б = Б + 1
			Консоль.Вывод("Б = ")
			Консоль.Вывод(Б)
			Консоль.Вывод("\н")
			
			поле В = 0
			пока В мш 5:
				В = В + 1
				Консоль.Вывод("В = ")
				Консоль.Вывод(В)
				Консоль.Вывод("\н")
				если В == 2:
					прервать прервать следующий
				Консоль.Вывод("Завершение цикла В\н")

			Консоль.Вывод("Завершение цикла Б\н")

		Консоль.Вывод("Завершение цикла А\н")

	Консоль.Вывод("Конец программы\н")
