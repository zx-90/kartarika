
стат метод Запустить():
	поле А = 5
	поле Б = 8
	
	если А бш 3:
		поле А = 3
		Консоль.Вывод(А)
		Консоль.Вывод("\н")
	иначе если Б бш 3:
		Консоль.Вывод("Условие выполнено, но напечатано быть НЕ должно\н")
	иначе:
		Консоль.Вывод("Эта строка не должна быть напечатана\н")
		
	если А бш 6:
		Консоль.Вывод("Эта строка не должна быть напечатана\н")
	иначе если Б бш 10:
		Консоль.Вывод("Эта строка также не должна быть напечатана\н")
	иначе если Б бш 5:
		// Первое условие в цепочке, которое выполнено.
		поле Б = 9
		Консоль.Вывод(Б)
		Консоль.Вывод("\н")
	иначе:
		Консоль.Вывод("Блок иначе также не распеатается\н")

	если А бш 6:
		Консоль.Вывод("Эта строка не должна быть напечатана\н")
	иначе если Б бш 10:
		Консоль.Вывод("Эта строка также не должна быть напечатана\н")
	иначе если А бш Б:
		Консоль.Вывод("Здесь условие также не выполнено\н")
	иначе:
		поле А = -3
		Консоль.Вывод(А)
		Консоль.Вывод("\н")

	Консоль.Вывод("Конец программы\н")
	Консоль.Вывод("А = ")
	Консоль.Вывод(А)
	Консоль.Вывод("\н")
	
	Консоль.Вывод("Б = ")
	Консоль.Вывод(Б)
	Консоль.Вывод("\н")
	