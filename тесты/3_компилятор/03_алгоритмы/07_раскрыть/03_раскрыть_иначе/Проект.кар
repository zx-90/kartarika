
стат метод Запустить():
	поле А = Строка?("Привет, мир!!!\н")
	раскрыть А:
		Консоль.Вывод(А)
	иначе:
		Консоль.Вывод("Эта строка не должна быть выведена\н")

	поле В = Строка?(Пусто)
	раскрыть В:
		Консоль.Вывод("Эта строка тоже не должна быть выведена\н")
	иначе:
		Консоль.Вывод("Пока!!!\н")
