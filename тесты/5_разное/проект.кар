класс расширяет Базовый

использовать Кар как Ат
использовать Кар.* как Ат2
использовать Кар.ФайловаяСистема.Путь

перечисление Цвет:
	Белый
	Чёрный
	Серый
	Красный
	Оранжевый
	Жёлтый
	Зелёный
	Голубой
	Синий
	Фиолетовый

поле цветКраски = Цвет.Белый

перечисление Источник:
	Файл
	Память
	Сеть

объединение Данные(Источник):
	Источник.Файл Файл = Кар.Файл.Открыть("/home/user/data.txt")
	Источник.Память Память = Кар.ПотокПамяти.Создать()
	Источник.Сеть Сеть = Кар.ТСП.Подключить("http://ttt.ru", 6305)

поле МойТипДанных = Источник.Файл
поле МоиДанные = Данные.Создать(МойТипДанных)

№ Открыто для чтения, закрыто для изменения.
поле Файл = Кар.Файл.Открыть("/home/user/ttt.txt").или(Кар.Файл.СоздатьВиртуальный())
№ Открыто для чтения и изменения
открытое поле Поле2 = -6
№ Закрыто для чтения и изменения
закрытое поле Поле3 = "Строка"

конструктор СоздатьПоТреугольникам(переменная Треугольник тр1, Треугольник тр2) расширяет Создать(тр1)
	поле Заголовок = Файл.Считать(10)

деструктор:
	Файл.Закрыть()

пустая стат метод Ударить(Воин Обороняющийся)
	
переопределяющая метод Ударить(Воин Обороняющийся):
	Обороняющийся.Здоровье -= Сила
	если Обороняющийся.Здоровье меньше 0:
		Обороняющийся.Статус = Состояние.Умер

стат метод запустить(массив(строка) Параметры) возвращает число:
	константа КоличествоПараметров = 3

	если (Параметры.Длина не равно КоличествоПараметров):
		Кар.Печатать("Количество параметров программы должно быть равно 3.%н")
		вернуть 1
	иначе:
		Кар.Печатать(
"Количество параметров равно 3. Всё ОК!!!
ывапвап ывап ыва пыва %"%н"
        )

	Кар.Печатать(номер, "%н")
	Кар.Печатать("Привет, мир!%н")
	пройти Параметры взять (Параметр, Номер):
		Кар.Печатать(Номер, ": ", Параметр, "%н")
	Кар.Печатать(Параметры.0)

	цикл Чиселка от 0 до (5 + 2):
		Кар.Печатать(Чиселка, "%н") № adsfadsf

	пока (Параметры.Длина меньше равно 5): № Комментарий
		Параметры.Добавить("%нПривет")
		! Многострочный комментарий
		Вот и вторая строка многострочного комментария
		!

	пройти Параметры взять Параметр:
		Кар.Печатать(Параметр, "%н")
		прервать
		пропустить
		
	блок:
		пустой

	поле Угол = дробное64(33,233)
	поле КрасныйЛи = нет
	
	№ Переменная со значением НУЛЬ (пусто).
	поле стр1 = строка?("Инициализированная строка")
	поле стр2 = строка?(пусто)
	раскрыть стр1, стр2:
		Кар.Печатать(стр1)
	иначе стр1:
		Кар.Печатать("Строка 1 не инициализирована")
	иначе стр2:
		Кар.Печатать("Строка 2 не инициализирована")
	иначе:
		Кар.Печатать("Неожиданная ошибка")
	стр2.или("Привет, мир!")
	
	поле Каталоги = дерево(строка).Создать("Корень")
	поле Дороги = граф(строка).Создать(граф.тип.матрица)
	
	!попытаться:
		поле А = 3 / 0
		Кар.Печатать(А)
	перехват ошибка:
		№ Ошибка. Номер, текст, стек.
		Кар.Печатать("Невозможно выполнить: " + ошибка.текст())
	!
	вернуть 0

тест МойТест():
	проверить(0 равно 0)
	проверить(0 равно 1)
